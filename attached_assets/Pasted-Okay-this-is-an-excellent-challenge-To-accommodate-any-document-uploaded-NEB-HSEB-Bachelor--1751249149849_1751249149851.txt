Okay, this is an excellent challenge\! To accommodate **any** document uploaded (NEB/HSEB, Bachelor's from Nepal, Bachelor's from India, etc.) and intelligently identify its level/type for extraction, we need a significantly more sophisticated approach, particularly in the Flask backend's processing logic.

The core idea is:

1.  **General OCR (Google Cloud Document AI's `OCR_PROCESSOR_V1` or `FORM_PARSER` for better table detection):** Get all the text and layout information.
2.  **Level/Type Identification (Flask Backend Heuristics):** Analyze the OCR'd text for keywords and structural clues to determine the document type (e.g., "HSEB Transcript," "Tribhuvan University Bachelor's," "Kuvempu University B.Sc.(IT) Marks Card").
3.  **Dynamic Data Extraction (Flask Backend):** Based on the identified type, apply specific parsing rules to extract relevant fields and subject data. This will involve more complex Python logic.
4.  **Flexible Database Storage (`JSONB`):** Store the extracted subject data and other variable fields in JSONB columns, allowing for different numbers of semesters/years and varying subject details.
5.  **Dynamic React Rendering:** The React frontend will receive the identified type and structured data, then render it appropriately.

Let's refine the prompt, focusing on the necessary changes for this advanced capability.

-----

**Replit AI Prompt: Universal Academic Transcript Analysis & Dynamic Extraction (React, Flask, PostgreSQL, Google Cloud Document AI)**

-----

**Goal:** My Replit app will analyze *any* uploaded academic transcript (image/PDF). The Flask backend will use Google Cloud Document AI to OCR the document, then *intelligently identify the transcript's type/level* (e.g., "Higher Secondary Transcript," "B.Sc.(IT) Marks Card," "Bachelor's Degree in Business Studies"). Based on this identified type, it will dynamically extract relevant fields and subject data, store it in a PostgreSQL database, and return a structured JSON to the React frontend for display.

**Key Challenges & Required Advanced Logic:**

  * **Dynamic Document Understanding:** The system must not assume a fixed template.
  * **Level/Type Classification:** Ability to distinguish between NEB/HSEB (10+2), Tribhuvan University Bachelor's, Kuvempu University Bachelor's, etc.
  * **Flexible Data Extraction:** Parsing different table structures and field names (e.g., "Secured Marks" vs "Sec") based on the identified type.

**Here's what I need help with, step-by-step:**

**1. Replit Project Setup:**
\* Create a new Replit project configured for a **React frontend** and a **Python Flask backend**.
\* **Backend `requirements.txt`:** Include `Flask`, `flask-cors`, `psycopg2-binary`, `google-cloud-documentai`.
\* **Frontend `package.json`:** Standard React setup with `react`, `react-dom`, and `axios` (for easier HTTP requests).

**2. PostgreSQL Database Schema (Updated for Flexibility):**
\* Explain how to integrate PostgreSQL with Flask on Replit, using **Replit Secrets** for the connection string.
\* **Database Schema:**
\* `CREATE TABLE transcripts (`
\* `id SERIAL PRIMARY KEY,`
\* `document_type TEXT NOT NULL,` -- E.g., 'Higher Secondary', 'Bachelors\_Tribhuvan\_University', 'Bachelors\_Kuvempu\_University'
\* `student_name TEXT,`
\* `institution_name TEXT,`
\* `issue_no TEXT,`
\* `registration_no TEXT,` -- Generic for Reg. No. / HSEB Reg No.
\* `transcript_id TEXT,` -- Generic for Academic Transcript No. / Marks Card No.
\* `date_of_issue DATE,` -- Converted to YYYY-MM-DD
\* `date_of_completion DATE,` -- New: for "Passed Year" or final completion date
\* `overall_percentage NUMERIC,`
\* `overall_division TEXT,`
\* `semesters_years_data JSONB,` -- **Crucial flexible column:** Array of objects, each representing a semester/year.
\* Each object in `semesters_years_data` should contain:
\* `level_name`: (e.g., "I SEMESTER", "Grade XI", "First Year")
\* `examination_period`: (e.g., "MAY/JUNE 2014", "2067")
\* `subjects`: Array of objects, each subject having:
\* `subject_code`: (e.g., "MGT.201", "Th. FUNDAMENTALS")
\* `subject_name`: (e.g., "English", "COMPUTER FUNDAMENTALS")
\* `full_marks`: (INTEGER)
\* `secured_marks`: (INTEGER)
\* `pass_marks`: (INTEGER, if available)
\* `internal_assessment_marks`: (INTEGER, if available from Kuvempu)
\* `remarks`: (TEXT, if available)
\* `semester_year_total_marks_secured`: (INTEGER)
\* `semester_year_grade_awarded`: (TEXT, if available)
\* `raw_extracted_json JSONB,` -- The full raw Document AI response.
\* `created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP`
\* `);`

**3. Flask Backend (`app.py`):**

```
* **Imports & App Initialization:**
    * Standard Flask, CORS (`flask_cors`), `os`, `request`, `jsonify`, `psycopg2`, `documentai_v1`.
* **Google Cloud Document AI Configuration:**
    * Set `GOOGLE_CLOUD_PROJECT_ID`, `GOOGLE_CLOUD_LOCATION` in Replit Secrets.
    * **Crucial:** Use `processor_id = 'form-parser'` or consider creating a **custom processor** if `form-parser` isn't accurate enough for diverse layouts. `form-parser` is generally better than `OCR_PROCESSOR_V1` for tables and key-value pairs without custom training. Let's start with `form-parser`.
    * Initialize `documentai.DocumentProcessorServiceClient` and `processor_name`.

* **PostgreSQL Database Connection:**
    * Standard `psycopg2` setup using Replit Secrets.

* **Analysis Route (`/api/analyze` - POST):**
    * **File Handling:** Standard file upload (`transcript_file`).
    * **Document AI Call:**
        * Send the `RawDocument` to Google Cloud Document AI's `form-parser`.
    * **Intelligent Document Type Identification & Dynamic Data Extraction (Python Logic):**
        * Access `result.document`.
        * **Step 1: Document Type/Institution Identification (Heuristics):**
            * Analyze `document.text` for keywords and phrases.
            * [cite_start]If `"KUVEMPU UNIVERSITY"` [cite: 1] is found: `document_type = "Bachelors_Kuvempu_University"`.
            * If `"Tribhuvan University"` is found: `document_type = "Bachelors_Tribhuvan_University"`.
            * If `"HIGHER SECONDARY EDUCATION BOARD"` and `"...GRADE XI..."` are found: `document_type = "Higher_Secondary_Nepal"`.
            * If none match, `document_type = "Unknown_Transcript"`.
        * **Step 2: Generic Field Extraction:**
            * Implement functions to extract common fields that might appear on any transcript, trying various labels Document AI might provide (e.g., "Student Name:", "Student's Name:", "Name:", etc.).
            * `student_name`
            * `institution_name` (identified in Step 1, or explicitly extracted if present as a field)
            * [cite_start]`issue_no` (try "Issue number:", "No:") [cite: 4]
            * [cite_start]`registration_no` (try "Reg. No.:", "HSEB Registration No.:") [cite: 3]
            * [cite_start]`transcript_id` (try "Academic Transcript No.:", "Marks Card. No:") [cite: 4]
            * [cite_start]`date_of_issue` (try "Date:", "Date of Issue:") [cite: 4] - **Crucial:** Convert to `YYYY-MM-DD` format for PostgreSQL. Use `datetime` parsing in Python.
            * [cite_start]`date_of_completion` (try "Passed Year:", "Date:") [cite: 7]
            * [cite_start]`overall_percentage` (try "Percentage:", "Total Marks Secured:") [cite: 8]
            * [cite_start]`overall_division` (try "Division:", "Grade Awarded:", "GRADE") [cite: 8]

        * **Step 3: Dynamic Subject/Semester/Year Data Extraction (`semesters_years_data`):**
            * This is the most complex part and will require conditional logic based on `document_type`.
            * **Iterate `document.pages[0].tables`:** Document AI's `form-parser` is good at table detection.
            * **For each identified `table`:**
                * **Identify Table Level/Semester:** Look for keywords in the table's surrounding text or within its header row (e.g., "I SEMESTER", "II SEMESTER", "GRADE XI", "GRADE XII", "First Year", "Second Year").
                * **Extract Table Headers:** Identify columns like "Subject", "Th/Pr", "Max", "Sec", "Int. Assmt. Max", "Remarks".
                * **Parse Table Rows:** For each row in the table, map the extracted cells to your `subject_code`, `subject_name`, `full_marks`, `secured_marks`, `pass_marks`, `internal_assessment_marks`, `remarks` fields. Handle variations in column names and order.
                * **Accumulate `semesters_years_data`:** Add a dictionary for each identified semester/year, containing its `level_name`, `examination_period`, and the `subjects` array.
            * **Handle Kuvempu-specifics:** The Kuvempu transcript has "Pr", "Th", "See", "Int. Assmt. Sec" columns. The parsing logic must adapt to these specific labels.
            * **Handle Tribhuvan/HSEB-specifics:** These have "Full Marks", "Pass Marks", "Marks Secured".

    * **Database Insertion:**
        * Insert all extracted fields, including the `semesters_years_data` (converted to JSON for `JSONB` column), into the `transcripts` table.
    * **Return JSON to Frontend:**
        * Return the structured `extracted_data` (including `document_type`, `student_name`, `semesters_years_data`, etc.) as a JSON response.
    * **Error Handling:** Robust `try-except` for file handling, Document AI calls, data parsing, and database operations. Return informative error JSON.

* **(Optional) Route (`/api/transcripts` - GET):**
    * A GET route to fetch all previously analyzed transcripts from PostgreSQL.
```

**4. React Frontend (`src/App.js` or similar):**

```
* **Component Structure:**
    * Standard file upload component.
    * Section to display currently extracted data.
    * (Optional) Section to display a list of past transcripts.
* **State Management:**
    * `selectedFile`, `extractedData`, `loading`, `error`, `pastTranscripts`.
* **API Interaction (`axios`):**
    * Use `axios.post` for `/api/analyze` and `axios.get` for `/api/transcripts`.
* **Dynamic Display Rendering (Crucial):**
    * When `extractedData` is received:
        * Display `extractedData.document_type` clearly at the top.
        * Display common fields like `student_name`, `institution_name`, `overall_percentage`, `overall_division`.
        * **Iterate through `extractedData.semesters_years_data`:**
            * For each item (representing a semester/year), dynamically create a heading (e.g., "Grade XI - 2067" or "I SEMESTER - MAY/JUNE 2014").
            * Inside each semester/year section, dynamically create a table for `subjects`, ensuring column headers adapt to whether `pass_marks` or `internal_assessment_marks` are present.
```

-----

This refined prompt outlines a comprehensive strategy for handling diverse transcript types. The most challenging part will be the **dynamic data extraction logic within the Flask backend** after the general Document AI OCR. This will involve careful pattern matching, keyword searching, and understanding how Document AI structures its `pages`, `tables`, `lines`, and `tokens` to reliably pull out the required fields from varying layouts.

**Self-Correction/Refinement for AI:**
If the initial attempt using `form-parser` or `OCR_PROCESSOR_V1` provides messy or inconsistent `document.entities` or `document.form_fields` for *your specific set of diverse transcripts*, the ultimate solution for high accuracy and robust dynamic extraction might involve:

  * **Training a Custom Document AI Processor:** This is the ideal but most effort-intensive solution. You'd upload examples of each transcript type (HSEB, Tribhuvan, Kuvempu), label the fields (e.g., `student_name`, `institution`, `semester_1_subjects`, `semester_2_subjects`), and let Google train a model. This model would then return very precise and structured JSON, simplifying your Flask parsing significantly.
  * **Using `Layout` or `Table` Processors if `form-parser` isn't enough:** Document AI has specialized processors for layout analysis and table extraction that might yield cleaner data than `OCR_PROCESSOR_V1` for your specific needs, even without full custom training.

For this prompt, we're sticking with `form-parser` for the extraction, then the Flask app doing the intelligent identification and mapping.